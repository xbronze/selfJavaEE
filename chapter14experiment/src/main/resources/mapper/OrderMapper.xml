<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dingli.chapter14experiment.dao.OrderMapper">

    <select id="getOrderByNum" parameterType="string" resultType="com.dingli.chapter14experiment.pojo.Order">
        select * from orders where number = #{number}
    </select>
</mapper>